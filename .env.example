# ----- Database -----
# Leave DATABASE_URL blank (or remove it) to use the default local SQLite database.
# Set it to a Postgres connection string to persist data in Postgres instead.
#
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ----- TMDB (optional) -----
# TMDB_API_KEY=your_tmdb_api_key
